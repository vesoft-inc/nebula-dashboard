const config = {
  connection: {
    ip: '127.0.0.1',
    port: 9669,
  },
  alias: {
    // you can change alias of instance here
    'ip:port': 'instance1'
  },
  chartBaseLine: {
  }
};

export default config;